enum ARSessionStatus {
  initial,
  planeSelected,
  bottomPointsSelected,
  heightSelected
}

// This is the type used by the popup menu edit session.
enum EditSessionItem { edit, delete }

class Languages {
  static const int english = 0;
  static const int vietnamese = 1;
}